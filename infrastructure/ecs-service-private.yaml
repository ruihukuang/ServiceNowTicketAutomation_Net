AWSTemplateFormatVersion: '2010-09-09'
Description: 'ECS Service in private subnet using VPC Endpoints'

Parameters:
  EnvironmentName:
    Type: String
  VpcId:
    Type: AWS::EC2::VPC::Id
  PrivateSubnetIds:
    Type: CommaDelimitedList
  TargetGroupArn:
    Type: String
  ECSSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
    Description: 'ECS Security Group ID from VPC stack'
  ClusterName:
    Type: String
  ECRRepositoryUri:
    Type: String
    Default: 997211577526.dkr.ecr.ap-southeast-2.amazonaws.com/servicenow_repo
  ContainerPort:
    Type: Number
    Default: 8080
  DesiredCount:
    Type: Number
    Default: 2
  Cpu:
    Type: String
    Default: '1024'
  Memory:
    Type: String
    Default: '4096'

Resources:
  # ECS Task Execution Role
  ECSExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${EnvironmentName}-ecs-execution-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
        - arn:aws:iam::aws:policy/CloudWatchLogsFullAccess  # ADDED
      Policies:
        - PolicyName: ECRPullAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - ecr:GetAuthorizationToken
                  - ecr:BatchCheckLayerAvailability
                  - ecr:GetDownloadUrlForLayer
                  - ecr:BatchGetImage
                  - ecr:DescribeRepositories
                  - ecr:ListImages
                  - ecr:DescribeImages
                Resource: '*'
              - Effect: Allow
                Action: sts:AssumeRole
                Resource: '*'
              - Effect: Allow
                Action:
                  - logs:*
                Resource: '*'
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:ListBucket
                Resource:
                  - arn:aws:s3:::prod-ap-southeast-2-starport-layer-bucket/*  # ECR layers bucket
                  - arn:aws:s3:::prod-ap-southeast-2-starport-layer-bucket

  # CloudWatch Log Group
  LogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /ecs/${EnvironmentName}/servicenow-api
      RetentionInDays: 30
      Tags:
        - Key: Environment
          Value: !Ref EnvironmentName
  
  # Network Diagnostic Task (Optional - for debugging)
  NetworkTestTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${EnvironmentName}-network-test
      NetworkMode: awsvpc
      RequiresCompatibilities: [FARGATE]
      Cpu: 256
      Memory: 512
      ExecutionRoleArn: !GetAtt ECSExecutionRole.Arn
      ContainerDefinitions:
        - Name: network-test
          Image: alpine/curl:latest
          Command:
            - sh
            - -c
            - |
              echo "Testing connectivity..."
              echo "Testing ECR API endpoint..."
              nslookup api.ecr.ap-southeast-2.amazonaws.com
              timeout 10 curl -v https://api.ecr.ap-southeast-2.amazonaws.com || echo "Failed"
              echo "Testing STS endpoint..."
              nslookup sts.ap-southeast-2.amazonaws.com
              timeout 10 curl -v https://sts.ap-southeast-2.amazonaws.com || echo "Failed"
              sleep 3600
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: network-test

  # Task Definition
  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub ${EnvironmentName}-servicenow-task
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref Cpu
      Memory: !Ref Memory
      ExecutionRoleArn: !GetAtt ECSExecutionRole.Arn
      TaskRoleArn: !GetAtt ECSExecutionRole.Arn
      ContainerDefinitions:
        - Name: servicenow-api
          Image: !Sub ${ECRRepositoryUri}:latest
          Essential: true
          PortMappings:
            - ContainerPort: !Ref ContainerPort
              Protocol: tcp
          Environment:
            - Name: ASPNETCORE_ENVIRONMENT
              Value: Production
            - Name: ConnectionStrings__DefaultConnection
              Value: DataSource=/app/data/database.db
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs
          HealthCheck:
            Command:
              - CMD-SHELL
              - curl -f http://localhost:8080/health || exit 1
            Interval: 30
            Timeout: 60
            Retries: 3
            StartPeriod: 300 #StartPeriod is the grace period before health checks start.
      Tags:
        - Key: Environment
          Value: !Ref EnvironmentName
        - Key: ImageSource
          Value: ecr-from-ghcr


  # ECS Service
  ECSService:
    Type: AWS::ECS::Service
    Properties:
      ServiceName: !Sub ${EnvironmentName}-servicenow-service
      Cluster: !Ref ClusterName
      LaunchType: FARGATE
      DesiredCount: !Ref DesiredCount
      DeploymentConfiguration:
        MaximumPercent: 200
        MinimumHealthyPercent: 100
      TaskDefinition: !Ref TaskDefinition
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED  # 关键：无公网IP
          Subnets: !Ref PrivateSubnetIds
          SecurityGroups:
            - !Ref ECSSecurityGroupId
      LoadBalancers:
        - ContainerName: servicenow-api
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !Ref TargetGroupArn
      HealthCheckGracePeriodSeconds: 180
      Tags:
        - Key: Environment
          Value: !Ref EnvironmentName
        - Key: Network
          Value: private-vpce

  # Auto Scaling
  ServiceScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    Properties:
      MaxCapacity: 4
      MinCapacity: !Ref DesiredCount
      ResourceId:  !Sub service/${ClusterName}/${EnvironmentName}-servicenow-service
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  ServiceScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${EnvironmentName}-cpu-scaling
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref ServiceScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        TargetValue: 50.0
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        ScaleOutCooldown: 60
        ScaleInCooldown: 300

Outputs:
  ServiceName:
    Description: 'ECS Service Name'
    Value: !Ref ECSService
  
  TaskDefinitionArn:
    Description: 'Task Definition ARN'
    Value: !Ref TaskDefinition
  
  LogGroupName:
    Description: 'CloudWatch Log Group Name'
    Value: !Ref LogGroup
  
  ECSExecutionRoleArn:
    Description: 'ECS Execution Role ARN'
    Value: !GetAtt ECSExecutionRole.Arn

  ClusterName:
    Description: 'ECS Cluster Name'
    Value: !Ref ClusterName